{{!< default}}

{{#post}}
<main id="site-main" class="site-main">
  <article class="article {{post_class}}">
  <h1>{{title}}</h1>
  <section class="dynamic-api-section">
    <div id="api-data-container">Loading data...</div>
  </section>
  </article>
</main>
{{/post}}

<script>
  document.addEventListener('DOMContentLoaded', async () => {
    try {
      // Replace with your actual external API endpoint
      const response = await fetch('https://glamglare-dev.uc.r.appspot.com/public/albums?upcoming=1');
      if (!response.ok) {
        throw new Error('Network response was not ok');
      }
      const data = await response.json();
      
      // Render the data inside the container
      const container = document.getElementById('api-data-container');
      container.innerHTML = data.result.map(item => `
        <div class="item">
          <h2>${item.name}</h2>
          <p>${item.artist}</p>
        </div>
      `).join('');
    } catch (error) {
      console.error('Error fetching API data:', error);
      document.getElementById('api-data-container').innerHTML = '<p>Failed to load data.</p>';
    }
  });
</script>
